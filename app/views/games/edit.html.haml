%header
  %h1 Editing game
#main
  %aside
    %header
      %h2 Rooms
    %nav
      %ul
        - @game.rooms.each do |room|
          = link_to room.name, edit_game_room_path(@game, room)
  = render 'form'

= link_to 'Show', @game
\|
= link_to 'Back', games_path
